%h1 Listing log_entries

%table
  %tr
    %th Batch
    %th Step
    %th Date
    %th Gravity
    %th Temperature
    %th Notes
    %th
    %th
    %th

  - @log_entries.each do |log_entry|
    %tr
      %td= log_entry.batch_id
      %td= log_entry.step
      %td= log_entry.date
      %td= log_entry.gravity
      %td= log_entry.temperature
      %td= log_entry.notes
      %td= link_to 'Show', log_entry
      %td= link_to 'Edit', edit_log_entry_path(log_entry)
      %td= link_to 'Destroy', log_entry, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Log entry', new_log_entry_path
